{
  "name": "resumate-ai",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix src/server\" \"npm run dev --prefix src/client\"",
    "install:all": "npm install --prefix src/server && npm install --prefix src/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
